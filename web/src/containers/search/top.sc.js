import {css, Input} from 'common'

const top = css`
  ${'' /* ${Panel} */}
  display: flex;
  flex-direction: column;
  align-items: center;
  width: 600px;

  ${Input} {
    margin-top: 8px;
  }
`

export default top
